---
phase: 11-fix-parallel-subagent-rendering-session-titles-and-left-panel-grouping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/index.ts
  - server/src/session-discovery.ts
  - client/src/utils/sessionName.ts
  - client/src/components/SessionList.tsx
  - client/src/utils/directoryGraphLayout.ts
  - client/src/components/nodes/SessionNode.tsx
  - client/src/components/DirectoryOverview.tsx
autonomous: true

must_haves:
  truths:
    - "Sessions display their initial command (excluding /clear) as the session title"
    - "Working directory appears below the session title as meta text"
    - "Sessions with no initial command fall back to directory name"
    - "Left panel shows sessions grouped by working directory"
    - "Directory Overview session nodes navigate to that session's detail view"
  artifacts:
    - path: "shared/src/index.ts"
      provides: "firstUserPrompt field on Session interface"
      contains: "firstUserPrompt"
    - path: "server/src/session-discovery.ts"
      provides: "Extraction of first non-/clear user message"
      contains: "firstUserPrompt"
    - path: "client/src/utils/sessionName.ts"
      provides: "getSessionTitle function"
      exports: ["getSessionTitle"]
    - path: "client/src/components/SessionList.tsx"
      provides: "Session title display and working directory grouping"
      contains: "getSessionTitle"
    - path: "client/src/utils/directoryGraphLayout.ts"
      provides: "sessionId in SessionNode data"
      contains: "sessionId"
    - path: "client/src/components/DirectoryOverview.tsx"
      provides: "onNodeClick handler for session navigation"
      contains: "onNodeClick"
  key_links:
    - from: "server/src/session-discovery.ts"
      to: "shared/src/index.ts"
      via: "firstUserPrompt field in Session"
      pattern: "firstUserPrompt"
    - from: "client/src/components/SessionList.tsx"
      to: "client/src/utils/sessionName.ts"
      via: "getSessionTitle import"
      pattern: "getSessionTitle"
    - from: "client/src/components/DirectoryOverview.tsx"
      to: "client/src/store/sessionStore.ts"
      via: "setSelectedSession + setViewMode on node click"
      pattern: "setSelectedSession.*setViewMode"
---

<objective>
Add session initial command as title, debug/fix left panel grouping, and make Directory Overview session nodes clickable for navigation.

Purpose: Three independent UI improvements that enhance session identification (users see meaningful titles instead of just directory names), ensure the left panel groups correctly, and allow navigating from directory overview to a specific session.

Output: Updated Session type with firstUserPrompt, SessionList showing command titles with cwd meta, Directory Overview with clickable session nodes.
</objective>

<execution_context>
@/home/botond/.claude/get-shit-done/workflows/execute-plan.md
@/home/botond/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-fix-parallel-subagent-rendering-session-titles-and-left-panel-grouping/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add firstUserPrompt to Session and extract during discovery</name>
  <files>shared/src/index.ts, server/src/session-discovery.ts, client/src/utils/sessionName.ts, client/src/components/SessionList.tsx</files>
  <action>
1. In `shared/src/index.ts`, add `firstUserPrompt?: string` to the `Session` interface (after the existing `cwd` field).

2. In `server/src/session-discovery.ts`, modify `parseSessionFile()` to extract the first user prompt:
   - After `const entries = parseJSONL(content);` and before building the session object
   - Loop through entries, find the first entry where `entry.type === 'user' && entry.role === 'user' && entry.content`
   - Trim the content. If it starts with `/clear`, skip it and continue to the next user entry
   - Set `firstUserPrompt` to the found content (no truncation on server side, the client handles display truncation)
   - Add `firstUserPrompt` to the Session object being returned (around line 411-424)

3. In `client/src/utils/sessionName.ts`, add a new exported function `getSessionTitle(session: Session): string`:
   - If `session.firstUserPrompt` exists and is non-empty, return it truncated to 60 chars with `...` suffix if longer
   - Otherwise, fall back to `getSessionDisplayName(session)` (existing function)
   - Import `Session` type from 'shared' (already imported)

4. In `client/src/components/SessionList.tsx`:
   - Import `getSessionTitle` from `../utils/sessionName`
   - In `SessionItem` component, change `displayTitle` to use `getSessionTitle(session)` instead of `displayName`
   - Keep `displayName` (from `getSessionDisplayNames`) as a fallback reference but it's no longer the primary title
   - Update the meta line: instead of just `formatTime(session.lastActivity)`, show the cwd display name AND the time. Format: `${getCwdDisplayName(session.cwd || '')} \u00B7 ${formatTime(session.lastActivity)}` (the middle dot separator)
   - This gives the two-line display: command title on top, directory + time below

5. Debug the SessionList grouping:
   - The `groupSessionsByCwd` function at line 108 looks correct. The issue is likely that sessions all share the same cwd value or have no cwd.
   - Add a `console.log` at the top of the `SessionList` function showing `sessions.map(s => ({ id: s.id.slice(0,8), cwd: s.cwd }))` so the user can verify what cwd values are coming through.
   - If the grouping code itself is correct (which it appears to be), the debug log will help identify the root cause. Leave the log in place for now; the user can remove it after verification.
  </action>
  <verify>
Run `npm run build` from the project root. Verify:
- No TypeScript errors
- `shared/src/index.ts` contains `firstUserPrompt?: string` in Session interface
- `server/src/session-discovery.ts` contains firstUserPrompt extraction logic
- `client/src/utils/sessionName.ts` exports `getSessionTitle`
- `client/src/components/SessionList.tsx` imports and uses `getSessionTitle`
  </verify>
  <done>
Sessions display their initial command (excluding /clear) as the title, with working directory and timestamp as meta below. Debug logging added to help diagnose grouping behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make Directory Overview session nodes navigate to session view</name>
  <files>client/src/utils/directoryGraphLayout.ts, client/src/components/nodes/SessionNode.tsx, client/src/components/DirectoryOverview.tsx</files>
  <action>
1. In `client/src/utils/directoryGraphLayout.ts`, add `sessionId` to the SessionNode data:
   - In the loop creating session nodes (around line 70-84), add `sessionId: session.id` to the `data` object
   - This requires updating the `SessionNodeData` interface to include `sessionId?: string`

2. In `client/src/components/nodes/SessionNode.tsx`:
   - Add `sessionId?: string` to the `SessionNodeData` interface (make it optional since not all SessionNodes are in directory view)
   - No click handler needed here -- the click will be handled at the ReactFlow level in DirectoryOverview

3. In `client/src/components/DirectoryOverview.tsx`:
   - Import `useSessionStore` (already imported) and use `setSelectedSession` and `setViewMode` selectors
   - Add an `onNodeClick` callback to the ReactFlow component:
     ```
     const handleNodeClick = useCallback((_: React.MouseEvent, node: Node) => {
       if (node.type === 'session' && node.data.sessionId) {
         setSelectedSession(node.data.sessionId);
         setViewMode('graph');
       }
     }, [setSelectedSession, setViewMode]);
     ```
   - Pass `onNodeClick={handleNodeClick}` to the ReactFlow component
   - Import `useCallback` from React (add to existing import)
   - Extract `setSelectedSession` and `setViewMode` from `useSessionStore` at the top of the component
  </action>
  <verify>
Run `npm run build` from the project root. Verify:
- No TypeScript errors
- `directoryGraphLayout.ts` passes `sessionId: session.id` in node data
- `SessionNode.tsx` has `sessionId?: string` in SessionNodeData
- `DirectoryOverview.tsx` has `onNodeClick` handler that calls `setSelectedSession` and `setViewMode`
  </verify>
  <done>
Clicking a session node in Directory Overview selects that session and switches to graph view, effectively navigating into that session's detail view.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no TypeScript errors
2. Session interface includes `firstUserPrompt?: string`
3. `getSessionTitle` function exists and is used in SessionList
4. SessionList shows command as title, cwd + time as meta
5. Directory Overview session nodes have sessionId in data and onNodeClick wired
6. Console log added to SessionList for grouping debug
</verification>

<success_criteria>
- Sessions display their initial command (excluding /clear) as the session title
- Working directory name appears below the title as secondary info
- Directory Overview session nodes are clickable and navigate to session detail view
- Build succeeds with no TypeScript errors
- Debug logging present to diagnose left panel grouping issues
</success_criteria>

<output>
After completion, create `.planning/phases/11-fix-parallel-subagent-rendering-session-titles-and-left-panel-grouping/11-01-SUMMARY.md`
</output>
