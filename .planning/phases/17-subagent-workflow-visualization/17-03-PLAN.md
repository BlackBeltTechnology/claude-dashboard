---
phase: 17-subagent-workflow-visualization
plan: 03
type: execute
wave: 1
depends_on: ["17-02"]
files_modified:
  - client/src/utils/graphLayout.ts
  - client/src/components/nodes/SubagentBoxNode.tsx
  - client/src/components/Toolbar.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Expanded subagent boxes group consecutive same-name tool calls into single cards (e.g., 'Read (12)' instead of 12 separate Read cards)"
    - "Expanded subagent boxes do not overlap neighboring main graph nodes"
    - "Toolbar expand/collapse buttons read 'Expand All' and 'Collapse All'"
  artifacts:
    - path: "client/src/utils/graphLayout.ts"
      provides: "Tool grouping applied to subagent internal nodes; corrected dagre dimensions for expanded boxes"
      contains: "groupConsecutiveToolCalls"
    - path: "client/src/components/nodes/SubagentBoxNode.tsx"
      provides: "Rendering for grouped tool cards with count badge; explicit width/height on expanded box matching dagre"
    - path: "client/src/components/Toolbar.tsx"
      provides: "Descriptive button labels for expand/collapse all"
      contains: "Expand All"
  key_links:
    - from: "client/src/utils/graphLayout.ts"
      to: "client/src/utils/groupingUtils.ts"
      via: "groupConsecutiveToolCalls() applied to subagent tool nodes"
      pattern: "groupConsecutiveToolCalls"
    - from: "client/src/utils/graphLayout.ts"
      to: "client/src/components/nodes/SubagentBoxNode.tsx"
      via: "dagre dimensions match CSS width/height on expanded box"
      pattern: "width.*height"
---

<objective>
Fix subagent box tool grouping, expanded box sizing/overlap, and toolbar button labels.

Purpose: Close UAT gaps 1 (tool grouping inside subagent boxes), 2 (expanded box overlapping neighbors), and 5 (toolbar label clarity). These are layout and rendering fixes that must land before the model node and navigation fixes in Plan 04.

Output: Subagent boxes with grouped tool cards, correct dagre-to-CSS dimension sync, and descriptive toolbar labels.
</objective>

<execution_context>
@/home/botond/.claude/get-shit-done/workflows/execute-plan.md
@/home/botond/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-subagent-workflow-visualization/17-01-SUMMARY.md
@.planning/phases/17-subagent-workflow-visualization/17-02-SUMMARY.md
@.planning/debug/gap1-missing-edges.md
@.planning/debug/gap2-box-overlap.md
@.planning/debug/gap5-toolbar-labels.md
@client/src/utils/graphLayout.ts
@client/src/utils/groupingUtils.ts
@client/src/components/nodes/SubagentBoxNode.tsx
@client/src/components/Toolbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply tool grouping to subagent internal nodes and fix dagre dimensions</name>
  <files>client/src/utils/graphLayout.ts</files>
  <action>
  **Part A: Apply tool grouping to subagent internal nodes**

  The main timeline groups tools via `groupConsecutiveToolCalls()` (line ~286) but subagent internal nodes are built from raw `extractToolCallSummaries()` output with no grouping. Fix this in BOTH the parallel (lines ~548-571) and sequential (lines ~675-698) code paths.

  After building the raw `toolCalls` array from `extractToolCallSummaries()`, convert the tool nodes into a format compatible with `groupConsecutiveToolCalls()` and apply it. Specifically:

  1. After calling `extractToolCallSummaries(subagent)`, create an array of pseudo-AnyNode objects from the tool calls (they need at minimum `type: 'tool'`, `toolName`, `id`, `state`, `timestamp`, `parentId`). Call `groupConsecutiveToolCalls()` on this array.

  2. When building the `internalNodes` array, instead of iterating raw `toolCalls`, iterate the grouped result:
     - If the item is a single ToolNode (type === 'tool'), create an internal node as before (type: 'tool', label: toolName, etc.)
     - If the item is a ToolGroup (type === 'tool-group'), create an internal node with type: 'tool', but set `label` to `${toolGroup.toolName} (${toolGroup.count})`, set `toolName` to the group's toolName, and set `nodeData` to the first tool node in the group (for detail panel clicks). Add a `count` field if needed.

  3. Update the `SubagentBoxNodeData.internalNodes` type to add an optional `count?: number` field for grouped tool cards.

  4. Update `toolCount` in the box data label to use the grouped count (number of groups/individual tools) instead of raw tool count. Keep the raw `toolCalls.length` as `toolCount` for the "(N tools)" label since that represents actual tool invocations.

  Apply this change identically to BOTH the parallel subagent code path (~lines 548-581) and the sequential subagent code path (~lines 675-698).

  **Part B: Fix dagre dimension calculation for expanded boxes**

  In `applyDagreLayout()` (~lines 819-831 and ~lines 854-865), the expanded box dimensions are:
  - width: `Math.max(280, nodeCount * 160)` -- this is OK but `nodeCount` should now reflect GROUPED internal nodes (which will be fewer)
  - height: `120` -- this is too small. Increase to `160` to account for: header (30px) + card row (60px) + padding (20px) + margins (14px) + scrollbar buffer (15px) + safety margin (21px)

  Update BOTH places where expanded dimensions are calculated (the setNode call and the position calculation).
  </action>
  <verify>Run `npm run build` from the project root. Zero TypeScript errors. Inspect the code to confirm: (1) groupConsecutiveToolCalls is called on subagent tool nodes in both parallel and sequential paths, (2) the internalNodes array contains grouped entries when consecutive same-name tools exist, (3) dagre expanded height is 160 not 120.</verify>
  <done>Subagent internal tool nodes are grouped via groupConsecutiveToolCalls. Dagre expanded box height is 160px. Both parallel and sequential code paths apply grouping identically.</done>
</task>

<task type="auto">
  <name>Task 2: Update SubagentBoxNode rendering for grouped tools and explicit expanded dimensions</name>
  <files>client/src/components/nodes/SubagentBoxNode.tsx</files>
  <action>
  **Part A: Render grouped tool cards**

  In the expanded view's internal nodes rendering (~lines 292-341), update the tool card rendering to show count when a tool node has `count > 1`:
  - Where currently `{node.type === 'tool' && node.toolName}` is rendered (~line 331), change to: `{node.type === 'tool' && node.label}` (the label already contains "ToolName (N)" for groups, or just "ToolName" for singles). This is the simplest change since graphLayout.ts now sets the label to include the count.

  **Part B: Set explicit width and height on expanded box CSS**

  In `styles.boxExpanded` (~lines 88-95), the expanded box has NO width or height. This causes a dagre-vs-DOM mismatch where dagre allocates space based on its formula but the DOM renders at a different size, causing overlap with neighboring nodes.

  The SubagentBoxNode needs to read dimensions from its data to match dagre. Since the data already contains `internalNodes`, compute the same formula used by dagre:
  - Add a computed width and height to the expanded box div's inline style (not in the static styles object, but in the JSX where styles are spread)
  - width: `Math.max(280, (data.internalNodes?.length || 3) * 160)` px
  - height: `160` px (matching the new dagre height)
  - Use `overflow: 'hidden'` on the expanded box to ensure no content exceeds the dagre allocation

  Specifically, in the expanded view div (~line 262-269), add to the inline style:
  ```
  width: `${Math.max(280, (data.internalNodes?.length || 3) * 160)}px`,
  height: '160px',
  overflow: 'hidden',
  ```

  This ensures the DOM element is exactly the size dagre allocated, preventing overlap.
  </action>
  <verify>Run `npm run build` from the project root. Zero TypeScript errors. Inspect SubagentBoxNode.tsx to confirm: (1) expanded view div has explicit width and height matching dagre formula, (2) tool cards display the label field which includes count for groups.</verify>
  <done>Expanded SubagentBoxNode has explicit width/height matching dagre dimensions. Tool cards show grouped labels like "Read (12)". No DOM-vs-dagre mismatch.</done>
</task>

<task type="auto">
  <name>Task 3: Rename toolbar expand/collapse labels</name>
  <files>client/src/components/Toolbar.tsx</files>
  <action>
  In `client/src/components/Toolbar.tsx`:
  - Line 222: Change `+ All` to `Expand All`
  - Line 233: Change `- All` to `Collapse All`

  Keep the existing `title` attributes ("Expand all agent boxes" / "Collapse all agent boxes") unchanged since they provide additional tooltip context.
  </action>
  <verify>Run `npm run build` from the project root. Zero TypeScript errors. Grep for "Expand All" and "Collapse All" in Toolbar.tsx to confirm labels changed.</verify>
  <done>Toolbar buttons read "Expand All" and "Collapse All" instead of "+ All" and "- All".</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. In graphLayout.ts, `groupConsecutiveToolCalls` is imported and called on subagent tool nodes in both parallel and sequential code paths
3. In graphLayout.ts, dagre expanded box height is 160 (not 120) in both dimension calculation locations
4. In SubagentBoxNode.tsx, expanded view has explicit `width` and `height` inline styles
5. In SubagentBoxNode.tsx, tool cards render `node.label` (which includes count for groups)
6. In Toolbar.tsx, buttons read "Expand All" and "Collapse All"
</verification>

<success_criteria>
- Expanded subagent boxes show grouped tool cards (e.g., "Read (12)") instead of one card per tool call
- Expanded boxes have explicit CSS dimensions matching dagre, preventing overlap with neighboring nodes
- Toolbar labels are descriptive: "Expand All" and "Collapse All"
- TypeScript builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/17-subagent-workflow-visualization/17-03-SUMMARY.md`
</output>
