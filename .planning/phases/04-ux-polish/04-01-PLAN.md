---
phase: 04-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/SessionList.tsx
autonomous: true

must_haves:
  truths:
    - "Sessions sharing the same working directory appear under a collapsible group header in the sidebar"
    - "Single-session directories render as normal session items (no unnecessary group wrapper)"
    - "Groups default to expanded, clicking the header toggles collapsed/expanded"
    - "Group header shows directory name and session count"
  artifacts:
    - path: "client/src/components/SessionList.tsx"
      provides: "Collapsible session groups by cwd"
      contains: "CollapsibleSessionGroup"
  key_links:
    - from: "SessionList"
      to: "session.cwd"
      via: "useMemo groupSessionsByCwd"
      pattern: "groupSessionsByCwd"
---

<objective>
Add collapsible workspace session grouping to the sidebar SessionList.

Purpose: When users have multiple Claude sessions in the same project directory, the sidebar becomes cluttered. Grouping by working directory with collapse/expand lets users manage visibility.

Output: SessionList.tsx updated with cwd-based grouping and collapsible group headers.
</objective>

<execution_context>
@/home/botond/.claude/get-shit-done/workflows/execute-plan.md
@/home/botond/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/components/SessionList.tsx
@client/src/utils/sessionName.ts
@shared/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cwd-based session grouping with collapsible headers to SessionList</name>
  <files>client/src/components/SessionList.tsx</files>
  <action>
Modify SessionList.tsx to group sessions by working directory and render collapsible group headers:

1. Add a `groupSessionsByCwd` function that takes a sorted Session[] and returns a Map<string, Session[]> where the key is `session.cwd || '__no_cwd__'`. Preserve the insertion order of the sorted array.

2. Add a `getCwdDisplayName(cwd: string)` helper that extracts the last path segment (e.g., `/home/user/my-project` -> `my-project`). Handle `__no_cwd__` as "Unknown Location" and empty/root as "root".

3. Add a `CollapsibleSessionGroup` component inside SessionList.tsx:
   - Props: `{ cwd: string; sessions: Session[]; displayNames: Map<string, string>; selectedSessionId: string | null; onSessionSelect: (id: string) => void }`
   - Uses `useState(true)` for `isExpanded` (default expanded)
   - Renders a group header div with: chevron indicator (down-triangle when expanded, right-triangle when collapsed), directory name from `getCwdDisplayName`, and count badge `(N)`
   - When expanded, renders child SessionItem components indented slightly via `paddingLeft`
   - Header is clickable to toggle expand/collapse
   - Style the group header with: `backgroundColor: '#0f3460'`, `padding: '8px 16px'`, `cursor: 'pointer'`, `borderBottom: '1px solid #16213e'`. Chevron in `fontSize: '10px'`, `color: '#888'`, `marginRight: '8px'`. Group title in `fontSize: '12px'`, `fontWeight: 600`, `color: '#93c5fd'`. Count in `fontSize: '11px'`, `color: '#6b7280'`.

4. Update the `SessionList` component's render logic:
   - Add `const sessionGroups = React.useMemo(() => groupSessionsByCwd(sortedSessions), [sortedSessions]);`
   - Replace the current `sortedSessions.map(...)` with iteration over `sessionGroups`:
     - For entries with 1 session: render a single `SessionItem` directly (no group wrapper)
     - For entries with 2+ sessions: render `CollapsibleSessionGroup`
   - Keep the existing "Sessions (N)" section title showing total count

5. Add hover state to group header (subtle background lightening on hover, similar to session items).

Do NOT move expansion state to Zustand -- this is UI-only local state per the research recommendations and anti-patterns section.
Do NOT change the SessionItem component or the displayNames logic.
Do NOT change the sorting logic (already sorts by lastActivity).
  </action>
  <verify>
Run `npm run build` from project root -- no TypeScript errors.
Visually verify: When multiple sessions share the same cwd, they appear under a collapsible group header with directory name and count. Clicking the header collapses/expands the group. Single-cwd sessions render without a group wrapper.
  </verify>
  <done>
Sessions sharing the same working directory are grouped under a collapsible header showing the directory name and count. Single sessions render normally. Groups default to expanded. Toggle works on click.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- SessionList renders grouped sessions with collapsible headers
- Single-session directories show no group header
- Expand/collapse toggles correctly on click
</verification>

<success_criteria>
- Sessions sharing the same cwd appear under a collapsible group header
- Group header shows directory name (last path segment) and session count
- Single sessions render without group wrapper
- Groups default to expanded, click toggles state
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-ux-polish/04-01-SUMMARY.md`
</output>
