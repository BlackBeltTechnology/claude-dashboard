---
phase: 20-advanced-node-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/store/sessionStore.ts
  - client/src/utils/graphLayout.ts
  - client/src/components/TreeView.tsx
autonomous: true

must_haves:
  truths:
    - "Graph view hides tool nodes whose toolName does not match the active tool filter"
    - "Graph view hides subagent boxes whose agentName does not match the active agent filter"
    - "Graph view hides user-prompt nodes whose text does not match the active prompt filter"
    - "Graph view hides model-output nodes whose content does not match the active model filter"
    - "Tree view applies the same content-level filters as graph view"
    - "When no content filter is set for a category, all nodes in that category are shown (backward compatible)"
  artifacts:
    - path: "client/src/store/sessionStore.ts"
      provides: "nodeTypeFilters state and actions"
      contains: "nodeTypeFilters"
    - path: "client/src/utils/graphLayout.ts"
      provides: "Content-level filtering in timeline processing"
      contains: "nodeTypeFilters"
    - path: "client/src/components/TreeView.tsx"
      provides: "Content-level filtering in tree timeline"
      contains: "nodeTypeFilters"
  key_links:
    - from: "client/src/store/sessionStore.ts"
      to: "client/src/utils/graphLayout.ts"
      via: "nodeTypeFilters parameter"
      pattern: "nodeTypeFilters.*Map"
    - from: "client/src/store/sessionStore.ts"
      to: "client/src/components/TreeView.tsx"
      via: "nodeTypeFilters selector"
      pattern: "nodeTypeFilters"
---

<objective>
Add content-level filtering state and logic so that each node category (tools, subagents, prompts, model) can be filtered by content — not just toggled on/off.

Purpose: Currently the filter chips only toggle entire categories. Users need to filter to specific tool names (e.g., only show Bash), specific agent names, prompt text, and model response content.

Output: Store state with per-category filter strings, graph layout filtering logic, and tree view filtering logic — all backward compatible (empty filter = show all in category).
</objective>

<execution_context>
@/home/botond/.claude/get-shit-done/workflows/execute-plan.md
@/home/botond/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/store/sessionStore.ts
@client/src/utils/graphLayout.ts
@client/src/components/TreeView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-category filter state to Zustand store</name>
  <files>client/src/store/sessionStore.ts</files>
  <action>
Add a `nodeTypeFilters` state field to the SessionStore interface as `Map<string, string>` where:
- Key is the category: 'tools', 'subagents', 'prompts', 'model'
- Value is the filter string (e.g., tool name like "Bash", agent name like "gsd-planner", or text search substring for prompts/model)

Add these actions:
- `setNodeTypeFilter(category: string, filter: string)` — sets filter for a category (empty string clears it)
- `clearNodeTypeFilter(category: string)` — removes filter for a category
- `clearAllNodeTypeFilters()` — clears all content filters

Initialize `nodeTypeFilters` as `new Map<string, string>()`.

Also add the new state and actions to the interface definition. The existing `hiddenNodeTypes` Set and `toggleNodeTypeVisibility` action remain unchanged — they control category-level visibility. The new `nodeTypeFilters` adds content-level filtering within visible categories.
  </action>
  <verify>TypeScript compiles: `cd /home/botond/claude-session-dashboard && npx tsc --noEmit -p client/tsconfig.json` (or build check)</verify>
  <done>Store has nodeTypeFilters Map with set/clear actions, existing hiddenNodeTypes unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Apply content filters in graph layout and tree view</name>
  <files>client/src/utils/graphLayout.ts, client/src/components/TreeView.tsx</files>
  <action>
**In graphLayout.ts:**

1. Add `nodeTypeFilters: Map<string, string>` parameter to `convertSessionToGraph()`, `convertSessionsToGraph()`, and `createLayoutedGraph()` functions (after `hiddenNodeTypes`), defaulting to `new Map()`.

2. In the timeline filtering section (where `premergeTimeline` is built from `processedTimeline`), after the existing `hiddenNodeTypes` filter, add a second filter pass for content-level filtering. For items that pass the category visibility check, additionally check:
   - For `tool` / `tool-group` items: if `nodeTypeFilters.get('tools')` is set, check if the toolName matches (case-insensitive contains). Filter out non-matching tool items.
   - For `subagent` items: if `nodeTypeFilters.get('subagents')` is set, check if the subagent's agentName or agentType matches (case-insensitive contains). Filter out non-matching subagents.
   - For `user-prompt` items: if `nodeTypeFilters.get('prompts')` is set, check if the promptText matches (case-insensitive contains). Filter out non-matching prompts.
   - For `model` / `model-group` items: if `nodeTypeFilters.get('model')` is set, check if the content matches (case-insensitive contains). Filter out non-matching model outputs.
   - For `skill` items: if `nodeTypeFilters.get('skills')` is set, check if skillName matches (case-insensitive contains).

3. Also apply the same content filtering to subagent internal nodes (`filteredInternalNodes`) — filter tool nodes inside expanded subagent boxes by toolName if the tools filter is active, and model nodes by content if model filter is active.

**In TreeView.tsx:**

1. In `buildSessionTimeline()`, add `nodeTypeFilters: Map<string, string>` parameter.

2. After the existing `hiddenNodeTypes` filter at the end of the function, add a content-level filter pass using the same logic as graphLayout (tool name match, subagent name match, prompt text match, model content match).

3. In the TreeView component, read `nodeTypeFilters` from the store and pass it to `buildSessionTimeline()`.

**Important:** When a category is in `hiddenNodeTypes` (hidden entirely), the content filter for that category is irrelevant. Content filters only apply to visible categories.
  </action>
  <verify>Build: `cd /home/botond/claude-session-dashboard && npm run build` passes without errors</verify>
  <done>Graph layout and tree view filter nodes by content when nodeTypeFilters is set; empty filters show all nodes in category (backward compatible)</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no TypeScript errors
2. Store exports nodeTypeFilters state and set/clear actions
3. graphLayout.ts accepts and applies nodeTypeFilters parameter
4. TreeView.tsx reads nodeTypeFilters from store and applies to timeline
</verification>

<success_criteria>
Content-level filtering logic is wired from store through graph layout and tree view. Setting a filter string for a category hides non-matching nodes. Empty filter = show all (no regression).
</success_criteria>

<output>
After completion, create `.planning/phases/20-advanced-node-filtering-by-type-agent-name-prompt-text-and-model-response-content/20-01-SUMMARY.md`
</output>
