---
phase: 06-notification-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/notifications.ts
  - server/src/index.ts
  - server/package.json
  - shared/src/index.ts
  - client/src/hooks/useNotifications.ts
  - client/src/components/Settings.tsx
autonomous: true

must_haves:
  truths:
    - "Notifications are browser-only — no server-side desktop notifications"
    - "Notification body shows working directory name (e.g., 'claude-session-dashboard') instead of session UUID or summary"
    - "Notification content is clear and actionable"
    - "Settings panel has a single browser notifications toggle (no desktop toggle)"
    - "node-notifier dependency is removed from server"
  artifacts:
    - path: "client/src/hooks/useNotifications.ts"
      provides: "Browser-only notification hook using getSessionDisplayName"
      contains: "getSessionDisplayName"
    - path: "client/src/components/Settings.tsx"
      provides: "Simplified settings with browser-only notification toggle"
    - path: "server/src/index.ts"
      provides: "Server entry point without NotificationManager"
    - path: "shared/src/index.ts"
      provides: "Shared types without NotificationPreferences"
  key_links:
    - from: "client/src/hooks/useNotifications.ts"
      to: "client/src/utils/sessionName.ts"
      via: "import getSessionDisplayName"
      pattern: "import.*getSessionDisplayName.*from.*sessionName"
    - from: "client/src/components/Settings.tsx"
      to: "localStorage"
      via: "browser notification preference stored locally"
      pattern: "localStorage.*claude-dashboard-browser-notifications"
---

<objective>
Simplify the notification system to browser-only and show working directory names instead of session UUIDs.

Purpose: Remove unnecessary server-side notification complexity (node-notifier) and make notification content meaningful by showing human-readable session names from Phase 3.
Output: Clean browser-only notification system with directory-based session names.
</objective>

<execution_context>
@/home/botond/.claude/get-shit-done/workflows/execute-plan.md
@/home/botond/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-notification-refinement/06-RESEARCH.md

Key existing files:
@server/src/notifications.ts (to be deleted)
@server/src/index.ts (remove NotificationManager wiring and API endpoints)
@shared/src/index.ts (remove NotificationPreferences type)
@client/src/hooks/useNotifications.ts (update session label to use getSessionDisplayName)
@client/src/components/Settings.tsx (remove desktop toggle and server API calls)
@client/src/utils/sessionName.ts (reuse getSessionDisplayName — already exists from Phase 3)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove server-side notification infrastructure</name>
  <files>
    server/src/notifications.ts
    server/src/index.ts
    server/package.json
    shared/src/index.ts
  </files>
  <action>
    1. DELETE `server/src/notifications.ts` entirely — the NotificationManager class and node-notifier dependency are no longer needed.

    2. In `server/src/index.ts`:
       - Remove the import: `import { NotificationManager } from './notifications.js';`
       - Remove the instantiation: `const notificationManager = new NotificationManager();` and `notificationManager.attach(sessionManager);`
       - Remove the export: remove `notificationManager` from the `export { wsManager, sessionManager, notificationManager };` line (keep wsManager and sessionManager)
       - Remove both API endpoints:
         - `app.get('/api/notifications/preferences', ...)` (lines 77-79)
         - `app.put('/api/notifications/preferences', ...)` (lines 82-99)
       - Remove `notificationManager.detach()` from both SIGINT and SIGTERM handlers

    3. In `shared/src/index.ts`:
       - Remove the `NotificationPreferences` interface (lines 150-153):
         ```
         export interface NotificationPreferences {
           desktop: boolean;
           browser: boolean;
         }
         ```

    4. Run `cd server && npm uninstall node-notifier` to remove the dependency from server/package.json and update package-lock.json.
  </action>
  <verify>
    - `server/src/notifications.ts` no longer exists
    - `grep -r "NotificationManager" server/src/` returns no results
    - `grep -r "node-notifier" server/` returns no results
    - `grep -r "NotificationPreferences" shared/src/` returns no results
    - `grep "notifications/preferences" server/src/index.ts` returns no results
    - `npm run build` from project root compiles without errors
  </verify>
  <done>
    Server has no notification logic. node-notifier is uninstalled. NotificationPreferences type is removed from shared. All server-side notification API endpoints are gone. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update client notifications to use directory names and simplify Settings</name>
  <files>
    client/src/hooks/useNotifications.ts
    client/src/components/Settings.tsx
  </files>
  <action>
    1. In `client/src/hooks/useNotifications.ts`:
       - Add import: `import { getSessionDisplayName } from '../utils/sessionName';`
       - Replace the session label logic (lines 67-69):
         FROM:
         ```
         const sessionLabel =
           session?.summary || `Session ${payload.sessionId.slice(0, 12)}...`;
         ```
         TO:
         ```
         const sessionName = session
           ? getSessionDisplayName(session)
           : `Session ${payload.sessionId.slice(0, 8)}...`;
         ```
       - Update the showNotification call (lines 71-74) to use `sessionName`:
         ```
         showNotification(
           'Session Waiting',
           `${sessionName} is waiting for input`
         );
         ```
       - No other changes needed — the hook already has correct permission handling, document.hasFocus() check, tag-based deduplication, and click-to-focus behavior.

    2. In `client/src/components/Settings.tsx`:
       - Remove `useSessionStore` import (no longer needed for this component — wait, check if it's used for session counts in Status section). Actually it IS used for session counts in the Status section (lines 171, 274-276). Keep the import.
       - Remove `const API_BASE = 'http://localhost:3847/api';` (line 4) — no longer calling server API from Settings.
       - Remove `desktopNotifications` state: `const [desktopNotifications, setDesktopNotifications] = useState(false);` (line 173)
       - Remove `notificationError` state: `const [notificationError, setNotificationError] = useState<string | null>(null);` (line 181)
       - Remove the useEffect that fetches server preferences on open (lines 186-203)
       - Remove the `handleDesktopNotificationChange` callback (lines 206-223)
       - Remove the Desktop Notifications toggle from the JSX — remove the entire `<div style={styles.settingRow}>` block containing "Desktop Notifications" and "Server-side desktop notifications" (lines 356-367)
       - Remove the `notificationError` display block (lines 382-396)
       - Update the Browser Notifications description text from "In-browser push notifications" to "Notify when a session is waiting for input" (more descriptive)
       - Add a small helper text below the browser toggle when `Notification.permission === 'denied'`:
         ```
         {typeof Notification !== 'undefined' && Notification.permission === 'denied' && (
           <div style={{ marginTop: '8px', fontSize: '12px', color: '#888' }}>
             Notifications blocked. Enable in browser settings.
           </div>
         )}
         ```
  </action>
  <verify>
    - `grep "getSessionDisplayName" client/src/hooks/useNotifications.ts` returns a match
    - `grep "Desktop Notifications" client/src/components/Settings.tsx` returns no results
    - `grep "notifications/preferences" client/src/components/Settings.tsx` returns no results
    - `grep "API_BASE" client/src/components/Settings.tsx` returns no results
    - `npm run build` from project root compiles without errors
  </verify>
  <done>
    Notification hook uses getSessionDisplayName to show "claude-session-dashboard is waiting for input" instead of UUID/summary. Settings panel has only a browser notifications toggle with clear description and denied-state guidance. No server API calls from Settings. Build succeeds.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Build check: `npm run build` succeeds from project root (all 3 workspaces compile)
2. No server notification references: `grep -r "NotificationManager\|node-notifier\|NotificationPreferences" server/ shared/` returns nothing
3. No desktop notification UI: `grep -r "Desktop Notification\|desktopNotification\|notifications/preferences" client/src/` returns nothing
4. Session name in notifications: `grep "getSessionDisplayName" client/src/hooks/useNotifications.ts` confirms integration
5. Browser-only notification toggle: Settings.tsx has only one notification toggle (browser)
</verification>

<success_criteria>
- Server has zero notification code — no NotificationManager, no node-notifier, no preferences API
- Client useNotifications hook shows working directory names (via getSessionDisplayName) in notification body
- Settings panel has single "Browser Notifications" toggle with localStorage persistence
- Settings panel shows guidance when notifications are blocked by browser
- Full project builds without errors (`npm run build`)
</success_criteria>

<output>
After completion, create `.planning/phases/06-notification-refinement/06-01-SUMMARY.md`
</output>
