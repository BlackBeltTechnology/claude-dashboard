---
phase: 09-clear-session-group-button-and-working-directory-graph-overview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/store/sessionStore.ts
  - client/src/components/SessionList.tsx
autonomous: true
must_haves:
  truths:
    - "User can see a Clear button when hovering over a working directory group in SessionList"
    - "Clicking Clear button shows confirmation dialog asking to remove all sessions in directory"
    - "After confirmation, sessions from that directory are hidden (filtered) from UI"
    - "Hidden sessions don't reappear when server sends updates"
    - "Other working directory groups remain visible and functional"
  artifacts:
    - path: "client/src/store/sessionStore.ts"
      provides: "State management for hiding sessions by working directory"
      contains: "hiddenCwds: Set<string>"
      exports: ["hideSessionsByCwd", "unhideAllCwds"]
    - path: "client/src/components/SessionList.tsx"
      provides: "UI for clear button with confirmation dialog"
      contains: "Clear button in CollapsibleSessionGroup"
      min_lines: 220
  key_links:
    - from: "CollapsibleSessionGroup component"
      to: "sessionStore.hideSessionsByCwd"
      via: "onClick handler with confirmation dialog"
      pattern: "handleClear.*hideSessionsByCwd"
    - from: "sessionStore.getFilteredSessions"
      to: "hiddenCwds filter"
      via: "filter sessions by cwd not in hiddenCwds"
      pattern: "hiddenCwds.*filter"
---

<objective>
Add a Clear button to working directory group headers in SessionList that hides all sessions from a specific directory. Uses Zustand filtering (not removal) to prevent WebSocket update conflicts. Button shows on hover with confirmation dialog to prevent accidental clears.
</objective>

<execution_context>
@/home/botond/.claude/agents/gsd-planner.md
@/home/botond/claude-session-dashboard/.planning/phases/09-clear-session-group-button-and-working-directory-graph-overview/09-RESEARCH.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-fix-tree-view/08-01-SUMMARY.md

# SessionList already groups sessions by working directory
# sessionStore already has filtering infrastructure
# Research shows filtering is better than removal to avoid WebSocket conflicts
</context>

<tasks>

<task type="auto">
  <name>Add hiddenCwds state and actions to sessionStore</name>
  <files>client/src/store/sessionStore.ts</files>
  <action>
    Add hiddenCwds: Set<string> to SessionStore interface and initial state.

    Add hideSessionsByCwd action that adds cwd to hiddenCwds set (filters in getter, doesn't remove from sessions array to avoid WebSocket conflicts).

    Add unhideAllCwds action that clears hiddenCwds set.

    Update getFilteredSessions to filter out sessions whose cwd is in hiddenCwds set (applies after existing filter/search logic).

    Export these new actions so SessionList can access them.
  </action>
  <verify>TypeScript compilation passes, no type errors for hiddenCwds in store</verify>
  <done>Store has hiddenCwds state with hide/unhide actions, getFilteredSessions applies hiddenCwds filter</done>
</task>

<task type="auto">
  <name>Add Clear button to SessionList group headers</name>
  <files>client/src/components/SessionList.tsx</files>
  <action>
    In CollapsibleSessionGroup component, add state for isHovered.

    Add Clear button to group header that only appears when isHovered is true (onMouseEnter/onMouseLeave).

    Implement handleClear function that:
    - Calls e.stopPropagation() to prevent group expand/collapse
    - Shows confirmation dialog with window.confirm() showing directory name and session count
    - If confirmed, calls sessionStore.hideSessionsByCwd(cwd)

    Style the Clear button appropriately (red color, small size) to indicate destructive action.

    Ensure button doesn't interfere with existing expand/collapse click handler.
  </action>
  <verify>Hovering over group header shows Clear button, clicking Clear shows confirmation dialog</verify>
  <done>Clear button appears on hover, shows confirmation dialog, calls hideSessionsByCwd on confirmation</done>
</task>

</tasks>

<verification>
1. Open SessionList in browser
2. Hover over any working directory group header - Clear button should appear
3. Click Clear button - confirmation dialog should appear with directory name and session count
4. Confirm - sessions from that directory should disappear from UI
5. Try to interact with other directory groups - they should still work normally
6. Hidden sessions should not reappear even after WebSocket updates (filtering, not removal)
</verification>

<success_criteria>
- Clear button visible on hover in SessionList group headers
- Confirmation dialog prevents accidental clears
- Sessions hidden by directory stay hidden (filtering prevents WebSocket conflicts)
- No regressions to existing SessionList functionality
</success_criteria>

<output>
After completion, create `.planning/phases/09-clear-session-group-button-and-working-directory-graph-overview/09-01-SUMMARY.md`
</output>
