---
phase: 09-clear-session-group-button-and-working-directory-graph-overview
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/nodes/DirectoryNode.tsx
  - client/src/utils/directoryGraphLayout.ts
  - client/src/components/DirectoryOverview.tsx
  - client/src/store/sessionStore.ts
  - client/src/components/ViewToggle.tsx
autonomous: true
must_haves:
  truths:
    - "User can select 'Directory' from view toggle alongside Tree and Graph options"
    - "Directory view shows working directories as primary nodes with sessions as children"
    - "Directory nodes show directory name and session count"
    - "Directory view uses horizontal layout with directories on left, sessions on right"
    - "MiniMap shows directory overview with directory nodes highlighted"
  artifacts:
    - path: "client/src/components/nodes/DirectoryNode.tsx"
      provides: "Custom node component for directory visualization"
      contains: "DirectoryNode component with session count"
      min_lines: 20
    - path: "client/src/utils/directoryGraphLayout.ts"
      provides: "Layout algorithm for directory overview graph"
      contains: "createDirectoryOverviewGraph function"
      min_lines: 60
    - path: "client/src/components/DirectoryOverview.tsx"
      provides: "Main directory overview component using React Flow"
      contains: "ReactFlow with DirectoryNode and SessionNode"
      min_lines: 30
    - path: "client/src/store/sessionStore.ts"
      provides: "View mode type extension for directory view"
      contains: "type ViewMode = 'tree' | 'graph' | 'directory'"
    - path: "client/src/components/ViewToggle.tsx"
      provides: "UI toggle for selecting directory view"
      contains: "Directory button in ViewToggle"
      min_lines: 30
  key_links:
    - from: "directoryGraphLayout"
      to: "dagre layout algorithm"
      via: "LR layout with directories and sessions"
      pattern: "rankdir.*LR"
    - from: "DirectoryOverview component"
      to: "sessionStore.viewMode"
      via: "rendered when viewMode === 'directory'"
      pattern: "viewMode.*directory"
    - from: "ViewToggle"
      to: "sessionStore.setViewMode"
      via: "onClick handler"
      pattern: "setViewMode.*directory"
---

<objective>
Create a new Directory Overview view that shows working directories as primary nodes with their sessions as children. Uses existing React Flow + dagre stack with horizontal layout (Directory → Session hierarchy). Integrates into existing view toggle system.
</objective>

<execution_context>
@/home/botond/.claude/agents/gsd-planner.md
@/home/botond/claude-session-dashboard/.planning/phases/09-clear-session-group-button-and-working-directory-graph-overview/09-RESEARCH.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-fix-tree-view/08-01-SUMMARY.md
@client/src/components/GraphView.tsx
@client/src/utils/graphLayout.ts

# GraphView already uses React Flow with dagre layout
# SessionNode component provides pattern for node creation
# ViewToggle already switches between tree and graph modes
</context>

<tasks>

<task type="auto">
  <name>Create DirectoryNode component</name>
  <files>client/src/components/nodes/DirectoryNode.tsx</files>
  <action>
    Create new DirectoryNode component similar to SessionNode pattern.

    Component should:
    - Accept DirectoryNodeData with label (directory name), sessionCount, and cwd
    - Render directory name prominently
    - Show session count below directory name
    - Have output handle on right side (for connecting to sessions)
    - Use dark theme colors consistent with other nodes (#16213e background, #93c5fd text)
    - Have larger dimensions than session nodes (180-200px width, 70-80px height)

    Export from nodes/index.ts alongside other node types.
  </action>
  <verify>DirectoryNode.tsx created, exported from index.ts, TypeScript compiles without errors</verify>
  <done>DirectoryNode component renders directory with session count, has proper handles for React Flow</done>
</task>

<task type="auto">
  <name>Create directoryGraphLayout utility</name>
  <files>client/src/utils/directoryGraphLayout.ts</files>
  <action>
    Create createDirectoryOverviewGraph function that:
    - Takes sessions and expandedGroups parameters
    - Groups sessions by cwd using Map<string, Session[]>
    - Creates DirectoryNode for each unique cwd with sessionCount
    - Creates SessionNode for each session under its directory
    - Creates edges from directory node to each session node
    - Applies dagre layout with LR (left-to-right) direction
    - Returns { nodes, edges } for React Flow

    Use dagre with nodesep: 100, ranksep: 150 for clean spacing.

    Use existing patterns from graphLayout.ts but adapt for directory → session hierarchy.
  </action>
  <verify>directoryGraphLayout.ts created, TypeScript types match React Flow Node/Edge</verify>
  <done>Layout function creates proper directory → session graph structure with dagre positioning</done>
</task>

<task type="auto">
  <name>Create DirectoryOverview component</name>
  <files>client/src/components/DirectoryOverview.tsx</files>
  <action>
    Create DirectoryOverview component similar to GraphView pattern.

    Component should:
    - Use ReactFlow with MiniMap, Controls, Background
    - Import DirectoryNode and SessionNode as nodeTypes
    - Call createDirectoryOverviewGraph to get nodes/edges
    - Use useMemo to memoize layout computation
    - Set fitView to show full directory overview on load
    - Use dark theme colors (#1a1a2e background)
    - Configure MiniMap with nodeColor callback to highlight directory nodes in distinct color

    Follow exact pattern from GraphView but with directory → session hierarchy.
  </action>
  <verify>DirectoryOverview.tsx created, renders without errors, displays directory graph</verify>
  <done>DirectoryOverview component renders directory hierarchy graph with proper React Flow integration</done>
</task>

<task type="auto">
  <name>Update ViewMode type and ViewToggle UI</name>
  <files>client/src/store/sessionStore.ts, client/src/components/ViewToggle.tsx</files>
  <action>
    In sessionStore.ts:
    - Update ViewMode type from 'tree' | 'graph' to 'tree' | 'graph' | 'directory'
    - Update loadViewMode() to handle 'directory' mode (default to 'tree' if not set)

    In ViewToggle.tsx:
    - Add third button for 'Directory' mode
    - Add active styling when viewMode === 'directory'
    - Add onClick handler calling setViewMode('directory')
    - Add title tooltip "Directory Overview"

    Ensure button styling matches existing tree/graph buttons.
  </action>
  <verify>ViewToggle shows three buttons (Tree, Graph, Directory), clicking Directory sets viewMode</verify>
  <done>ViewMode supports directory, ViewToggle UI includes Directory button</done>
</task>

</tasks>

<verification>
1. Open dashboard and click Directory button in ViewToggle
2. Should see directory view with working directories as primary nodes
3. Each directory should show session count
4. Sessions should appear connected to their parent directory
5. MiniMap should show directory overview with directory nodes highlighted
6. Switch between Tree/Graph/Directory views - all should work
7. Layout should be horizontal (directories left, sessions right)
</verification>

<success_criteria>
- Directory view selectable from ViewToggle
- Directory nodes show directory names and session counts
- Sessions connected to parent directories in horizontal layout
- MiniMap optimized for directory view
- No regressions to existing tree/graph views
</success_criteria>

<output>
After completion, create `.planning/phases/09-clear-session-group-button-and-working-directory-graph-overview/09-02-SUMMARY.md`
</output>
