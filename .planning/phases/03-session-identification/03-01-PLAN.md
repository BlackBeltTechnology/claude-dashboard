---
phase: 03-session-identification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/index.ts
  - server/src/jsonl-parser.ts
  - server/src/session-discovery.ts
  - client/src/utils/sessionName.ts
  - client/src/components/SessionList.tsx
autonomous: true

must_haves:
  truths:
    - "User sees working directory name (e.g., 'claude-session-dashboard') in session sidebar list instead of UUID"
    - "Sessions ordered by most recent activity first in sidebar"
    - "Selected session highlighted with background color change only"
    - "Timestamp shown as secondary info below session name"
    - "When multiple sessions share same directory name, user sees disambiguated names with #1, #2 suffix"
  artifacts:
    - path: "shared/src/index.ts"
      provides: "Session type with cwd field"
      contains: "cwd?: string"
    - path: "server/src/jsonl-parser.ts"
      provides: "cwd extraction from JSONL entries"
      contains: "cwd"
    - path: "server/src/session-discovery.ts"
      provides: "cwd propagation to Session objects"
      contains: "cwd"
    - path: "client/src/utils/sessionName.ts"
      provides: "Display name derivation and disambiguation"
      exports: ["getSessionDisplayName", "getSessionDisplayNames"]
    - path: "client/src/components/SessionList.tsx"
      provides: "Sidebar showing directory names instead of UUIDs"
  key_links:
    - from: "server/src/jsonl-parser.ts"
      to: "shared/src/index.ts"
      via: "cwd field on ParsedEntry"
      pattern: "cwd"
    - from: "server/src/session-discovery.ts"
      to: "Session object"
      via: "cwd assignment in parseSessionFile"
      pattern: "session\\.cwd|cwd:"
    - from: "client/src/utils/sessionName.ts"
      to: "Session.cwd"
      via: "path.basename extraction"
      pattern: "cwd.*split|lastSegment|basename"
    - from: "client/src/components/SessionList.tsx"
      to: "client/src/utils/sessionName.ts"
      via: "import and call getSessionDisplayNames"
      pattern: "getSessionDisplayNames"
---

<objective>
Add working directory extraction to the data pipeline and display human-readable session names in the sidebar.

Purpose: Users currently see truncated UUIDs or summaries as session identifiers, which are meaningless. Working directory names (e.g., "claude-session-dashboard") are immediately recognizable and match what users see in their terminal/editor.

Output: Session objects carry `cwd` field from server to client. Sidebar displays directory name with disambiguation for duplicates. Sessions sorted by most recent activity.
</objective>

<execution_context>
@/home/botond/.claude/get-shit-done/workflows/execute-plan.md
@/home/botond/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-session-identification/03-CONTEXT.md
@shared/src/index.ts
@server/src/jsonl-parser.ts
@server/src/session-discovery.ts
@client/src/components/SessionList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cwd field to data pipeline (shared types, JSONL parser, session discovery)</name>
  <files>
    shared/src/index.ts
    server/src/jsonl-parser.ts
    server/src/session-discovery.ts
  </files>
  <action>
    1. In `shared/src/index.ts`:
       - Add `cwd?: string` field to the `Session` interface (after `gitBranch` field)
       - This is the working directory path extracted from JSONL entries

    2. In `server/src/jsonl-parser.ts`:
       - Add `cwd?: string` to the `ParsedEntry` interface
       - In the `parseLine` function, copy `raw.cwd` to `parsed.cwd` (the `RawJSONLEntry` already has `cwd?: string`)
       - In the `extractMetadata` function: add `cwd?: string` to `SessionMetadata` interface, and extract it by finding the first entry with a non-empty `cwd` field (similar pattern to how `gitBranch` is extracted)

    3. In `server/src/session-discovery.ts`:
       - In `parseSessionFile`: read `metadata.cwd` and assign it to the Session object as `cwd: indexEntry?.projectPath || metadata.cwd`
         (The `SessionIndexEntry` has `projectPath?: string` which is the same concept)
       - In `discoverSubagents`: assign `cwd: session.cwd` to subagent sessions (they inherit parent's cwd since subagents work in the same directory)

    Important: The `cwd` field in JSONL entries contains the FULL path (e.g., "/home/user/projects/my-app"). We store the full path in Session.cwd. The client-side utility will extract the last segment for display.

    Do NOT change any function signatures or break existing callers. All additions are optional fields.
  </action>
  <verify>
    Run `npm run build` from the project root. All three workspaces (shared, server, client) must compile without errors. The new `cwd` field should be visible in the compiled types.
  </verify>
  <done>
    Session interface has `cwd?: string`. ParsedEntry extracts cwd from JSONL. SessionMetadata includes cwd. parseSessionFile populates Session.cwd from index entry's projectPath or JSONL metadata. Subagents inherit parent cwd. All existing code still compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create sessionName utility and update sidebar to show directory names</name>
  <files>
    client/src/utils/sessionName.ts
    client/src/components/SessionList.tsx
  </files>
  <action>
    1. Create `client/src/utils/sessionName.ts` with two exported functions:

       `getSessionDisplayName(session: Session): string`
       - If `session.cwd` exists: extract last path segment (split by '/' and take last non-empty element)
       - If no cwd: fall back to `session.summary` if available, then fall back to first 8 chars of session.id + "..."
       - Return the display name string

       `getSessionDisplayNames(sessions: Session[]): Map<string, string>`
       - For each session, compute the base display name via getSessionDisplayName
       - Count occurrences of each base name
       - For names that appear more than once: append " #1", " #2" etc., ordered by session.createdAt (earliest gets #1)
       - Return a Map from session.id to the final disambiguated display name
       - Single-occurrence names get no suffix

    2. Update `client/src/components/SessionList.tsx`:

       **Sorting change (LOCKED DECISION):** Sessions ordered by most recent activity first. Replace the current sort logic (which puts active first, then waiting, then by lastActivity) with a simple sort by `lastActivity` descending. This means the most recently active session is always at the top regardless of state.

       **Display name:** In the `SessionList` component, call `getSessionDisplayNames(sessions)` in a useMemo to build the display name map. Pass the display name to each `SessionItem`.

       **SessionItem updates:**
       - `displayTitle` should use the display name from the map (not summary or truncateId)
       - `displayMeta` should show ONLY the formatted timestamp (e.g., "2 min ago" or "Jan 15"). Remove the git branch from the meta line.
       - **Selected session styling (LOCKED DECISION):** Keep the existing highlight-only approach (background color change via `sessionItemSelected` style). Remove the status dot indicator entirely from SessionItem — the colored dot to the left of each session name should be removed. Keep the left border accent on the selected item.
       - UUID is hidden completely — never shown to user

       Keep the existing `formatTime` function. Remove the `truncateId` function (no longer needed).

    Important: Do NOT change the SessionList export name or the Zustand store interface. Only change the presentation layer.
  </action>
  <verify>
    Run `npm run build` from the project root — must compile without errors. Then run `npm run dev` and check the browser sidebar:
    1. Sessions should show directory names (or fallback to summary/truncated ID if no cwd)
    2. Sessions should be sorted by most recent activity (not by state)
    3. Selected session should have background highlight only (no status dot)
    4. Timestamp should appear below the session name
  </verify>
  <done>
    sessionName.ts exports getSessionDisplayName and getSessionDisplayNames. SessionList shows directory-derived names with disambiguation for duplicates. Sessions sorted by lastActivity descending. No status dots. Timestamp as secondary info. UUID never visible.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes for all workspaces
2. Start dev server (`npm run dev`) and verify sidebar:
   - Session names derived from working directory paths
   - Duplicate directory names disambiguated with #1, #2 suffix
   - Sessions ordered by most recent activity (most recent first)
   - Selected session highlighted with background color only
   - Timestamp shown below session name
   - No UUIDs visible anywhere in the sidebar
</verification>

<success_criteria>
- SESS-01 satisfied: Sessions display working directory name in sidebar
- SESS-02 satisfied: Same directory names disambiguated with #N suffix
- Session.cwd field populated from JSONL data pipeline
- All existing functionality preserved (graph view, tree view, drill-down panel)
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-identification/03-01-SUMMARY.md`
</output>
