---
phase: 03-session-identification
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - client/src/utils/graphLayout.ts
  - client/src/components/TreeNode.tsx
autonomous: true

must_haves:
  truths:
    - "User sees working directory name in session nodes in graph view instead of UUID"
    - "User sees working directory name in session nodes in tree view instead of UUID"
    - "Parent sessions show directory name, subagent/child sessions show their agent type"
  artifacts:
    - path: "client/src/utils/graphLayout.ts"
      provides: "Graph session nodes labeled with directory name"
      contains: "getSessionDisplayName"
    - path: "client/src/components/TreeNode.tsx"
      provides: "Tree session nodes labeled with directory name"
      contains: "getSessionDisplayName"
  key_links:
    - from: "client/src/utils/graphLayout.ts"
      to: "client/src/utils/sessionName.ts"
      via: "import getSessionDisplayName"
      pattern: "import.*getSessionDisplayName.*sessionName"
    - from: "client/src/components/TreeNode.tsx"
      to: "client/src/utils/sessionName.ts"
      via: "import getSessionDisplayName"
      pattern: "import.*getSessionDisplayName.*sessionName"
---

<objective>
Update graph and tree view labels to show working directory names instead of UUIDs for session nodes.

Purpose: The sidebar (Plan 01) now shows directory names, but graph and tree views still show "Session abc12345..." for session nodes. This plan makes labeling consistent across all views. Per LOCKED DECISION: parent sessions show directory name, subagent/child sessions show their agent type (e.g., "Explore", "Bash").

Output: Graph view session nodes and tree view session rows display directory-derived names. Subagent nodes show agent type label.
</objective>

<execution_context>
@/home/botond/.claude/get-shit-done/workflows/execute-plan.md
@/home/botond/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-session-identification/03-CONTEXT.md
@.planning/phases/03-session-identification/03-01-SUMMARY.md
@client/src/utils/graphLayout.ts
@client/src/components/TreeNode.tsx
@client/src/utils/sessionName.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update graph view session labels to use directory names</name>
  <files>
    client/src/utils/graphLayout.ts
  </files>
  <action>
    In `client/src/utils/graphLayout.ts`:

    1. Import `getSessionDisplayName` from `../utils/sessionName`

    2. In `convertSessionToGraph` function, update the session node label:
       - Current: `session.summary || 'Session ${session.id.slice(0, 8)}...'`
       - New: `getSessionDisplayName(session)`
       - This applies to the `sessionNode` (line ~70) where `data.label` is set

    3. For subagent sessions (the `isSubagent` branch around line ~82):
       - Current label: `session.summary || 'Subagent ${session.id.slice(0, 8)}...'`
       - New label: Keep showing agent type or description. The subagent Session objects don't have meaningful cwd (they inherit parent's). Use the existing pattern but clean up: show the description if available, otherwise "Task Agent"
       - Per LOCKED DECISION: subagent/child sessions show their agent type, not directory name

    4. In the `getNodeLabel` helper function (line ~36):
       - For `case 'session'`: change from `node.summary || 'Session ${node.sessionId.slice(0, 8)}'` to a more meaningful label. However, this function receives AnyNode, not Session, so it cannot access cwd. Since SessionNode in graph data already has label set via convertSessionToGraph, this getNodeLabel is only used if something else calls it. Keep it as-is or update to show a cleaner fallback.

    Important: The `convertSessionToGraph` function is where the actual label for graph nodes is set via `data.label`. That's the critical change. The `getNodeLabel` helper is a secondary concern.

    Do NOT change node dimensions, colors, edge styling, or the dagre layout configuration. Only change label text.
  </action>
  <verify>
    Run `npm run build` -- must compile without errors. Run `npm run dev` and check the graph view:
    1. Session root nodes should show directory name (e.g., "claude-session-dashboard")
    2. Subagent nodes should show agent type/description
    3. All other node types (tool, skill, tool-group) unchanged
  </verify>
  <done>
    Graph view session nodes display directory name from cwd. Subagent nodes show agent type. All other graph elements unchanged. Builds without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tree view session labels to use directory names</name>
  <files>
    client/src/components/TreeNode.tsx
  </files>
  <action>
    In `client/src/components/TreeNode.tsx`:

    1. Import `getSessionDisplayName` from `../utils/sessionName`
    2. Import `Session` type from `shared` (if not already imported -- it is already imported)

    3. In the `getNodeLabel` function:
       - For the Session branch (the `else` block at the bottom, around line ~129-131):
         Current: `node.summary || 'Session ${node.id.slice(0, 8)}...'`
         New: `getSessionDisplayName(node)` -- this works because Session has the same shape expected by the utility

       - For `case 'session'` (which handles SessionNode from AnyNode, line ~113-114):
         This is for SessionNode type in AnyNode union. SessionNode doesn't have `cwd`. Keep using summary fallback: `node.summary || 'Session ${node.sessionId.slice(0, 8)}...'`. This case is rarely hit since root sessions come through as Session objects, not SessionNode.

       - For `case 'subagent'` (line ~121-122):
         Current: `'Subagent: ${node.agentType}'`
         Per LOCKED DECISION, child sessions show agent type. Keep this as-is.

    4. For Session objects rendered as tree roots (the else branch):
       These are the main session entries in the tree. Using `getSessionDisplayName(node)` will show the directory name.

    Important: The TreeView component passes Session objects for root sessions and subagent Session objects for subagents. Both go through `getNodeLabel`. The Session branch uses getSessionDisplayName which extracts cwd -> directory name. Subagent Session objects will inherit parent cwd but we want them to show agent type -- however subagent Sessions go through a different code path (they're rendered via the subagent node in AnyNode, not as Session objects directly unless they are nested via session.subagents). Check the TreeView rendering: subagents from `session.subagents` are rendered as Session objects. So we need `getSessionDisplayName` to handle subagent sessions gracefully. Since subagent sessions have the same cwd as parent, they'd show the same directory name -- that's not ideal. To fix this, check: if the Session object has a short id (7 chars like "a8818a4" for subagent agent IDs) AND no unique cwd, treat it as a subagent and show "Task Agent" or similar. OR, better approach: TreeView already wraps subagents differently -- check how TreeView renders them.

    Looking at TreeView.tsx lines ~218-219: subagents are rendered via `renderNode(subagent, depth + 1, nodeKey)` where subagent is a Session object. These go through getNodeLabel's Session branch. The `getNodeIcon` function already returns the session folder icon for these.

    Best approach for subagent Sessions in tree: In `getNodeLabel`, when handling a Session object (the else branch), check if the session looks like a subagent (its id is short / doesn't look like a UUID). If it appears to be a subagent session, show "Task Agent" or the summary if available. Otherwise use getSessionDisplayName.

    Specifically: Add a simple check -- if `node.id.length < 20` (subagent IDs are like "a8818a4", ~7 chars, while session UUIDs are 36 chars), show `node.summary || 'Task Agent'` instead of the directory name. This avoids subagents showing the same directory name as their parent.
  </action>
  <verify>
    Run `npm run build` -- must compile without errors. Run `npm run dev` and check the tree view:
    1. Root session nodes should show directory name (e.g., "claude-session-dashboard")
    2. Subagent session entries should show "Task Agent" or their summary, not the parent's directory name
    3. All other node types (tool, skill, message, tool-group) unchanged
  </verify>
  <done>
    Tree view session labels show directory name from cwd. Subagent sessions show agent type or summary. All other tree elements unchanged. SESS-03 requirement satisfied: session nodes in graph/tree views show working directory name.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes for all workspaces
2. Start dev server (`npm run dev`) and verify:
   - Graph view: session root nodes show directory name, subagent nodes show agent type
   - Tree view: session root entries show directory name, subagent entries show agent type
   - Sidebar (from Plan 01): still shows directory names consistently
   - All three views use the same naming for root sessions (consistent experience)
   - Tool groups, tool nodes, skill nodes, message nodes all unchanged
</verification>

<success_criteria>
- SESS-03 satisfied: Session nodes in graph/tree views show working directory name
- Parent sessions show directory name across all views
- Subagent/child sessions show agent type across all views
- Consistent naming between sidebar, graph view, and tree view
- All existing functionality preserved (grouping, drill-down, expansion)
</success_criteria>

<output>
After completion, create `.planning/phases/03-session-identification/03-02-SUMMARY.md`
</output>
