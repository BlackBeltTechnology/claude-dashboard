---
phase: 12-navigation-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/store/sessionStore.ts
  - client/src/components/Layout.tsx
  - client/src/components/Toolbar.tsx
  - client/src/components/FilterBar.tsx
  - client/src/App.tsx
autonomous: true

must_haves:
  truths:
    - "User opens dashboard and sees directory graph as the main view with no left sidebar"
    - "User sees a toolbar above the graph with filter controls and session count in directory view"
    - "User sees a toolbar with back button, session title, tree toggle, and session switcher in session timeline view"
    - "User can click a session node in directory graph and instantly sees that session's timeline graph"
    - "User can click back button in toolbar to return to directory overview"
    - "User can filter active/archived sessions from the directory overview toolbar"
  artifacts:
    - path: "client/src/store/sessionStore.ts"
      provides: "Navigation state (navigationView, treePanelOpen, focusedNodeId, currentDirectoryCwd) and atomic actions (enterSession, exitToDirectory, toggleTreePanel, setFocusedNode)"
      contains: "navigationView"
    - path: "client/src/components/Layout.tsx"
      provides: "Sidebarless layout with toolbar header and center content area"
      min_lines: 30
    - path: "client/src/components/Toolbar.tsx"
      provides: "Conditional toolbar: directory view shows filters + count; session view shows back + title + tree toggle + session switcher"
      min_lines: 60
    - path: "client/src/components/FilterBar.tsx"
      provides: "Active/Archived checkboxes without search input (search removed per user decision)"
      min_lines: 20
    - path: "client/src/App.tsx"
      provides: "Root component using new navigation model, no sidebar, no ViewToggle"
      min_lines: 50
  key_links:
    - from: "client/src/components/Toolbar.tsx"
      to: "client/src/store/sessionStore.ts"
      via: "useSessionStore selectors for navigationView, exitToDirectory, toggleTreePanel, enterSession"
      pattern: "useSessionStore.*navigationView|exitToDirectory|toggleTreePanel"
    - from: "client/src/App.tsx"
      to: "client/src/store/sessionStore.ts"
      via: "navigationView controls which view renders (directory vs session-timeline)"
      pattern: "navigationView.*directory|session-timeline"
    - from: "client/src/components/Layout.tsx"
      to: "client/src/components/Toolbar.tsx"
      via: "Layout renders Toolbar in header"
      pattern: "Toolbar"
---

<objective>
Add navigation state to the Zustand store, restructure the layout to remove the left sidebar, build a conditional toolbar, and wire the directory-to-session navigation flow.

Purpose: Transform the dashboard from a sidebar-centric layout to a graph-centric layout where directory overview is the landing view and session timeline is the drill-down view, per user's locked decisions.

Output: Working navigation skeleton - user can see directory graph as main view, click sessions to enter timeline, and click back to return. Toolbar adapts to current view context.
</objective>

<execution_context>
@/home/botond/.claude/get-shit-done/workflows/execute-plan.md
@/home/botond/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-navigation-refactor/12-CONTEXT.md
@.planning/phases/12-navigation-refactor/12-RESEARCH.md
@client/src/store/sessionStore.ts
@client/src/components/Layout.tsx
@client/src/components/FilterBar.tsx
@client/src/App.tsx
@client/src/components/DirectoryOverview.tsx
@client/src/components/GraphView.tsx
@client/src/components/ViewToggle.tsx
@client/src/components/SessionList.tsx
@client/src/utils/sessionName.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Zustand store with navigation state and actions</name>
  <files>client/src/store/sessionStore.ts</files>
  <action>
Add navigation state fields and atomic actions to the existing SessionStore interface and implementation:

**New state fields:**
- `navigationView: 'directory' | 'session-timeline'` - initialized to `'directory'` (directory is the landing view per user decision)
- `treePanelOpen: boolean` - initialized to `false`
- `focusedNodeId: string | null` - initialized to `null` (for tree-to-graph sync, wired in plan 02)
- `currentDirectoryCwd: string | null` - initialized to `null` (for session switcher scoping)

**New actions:**
- `enterSession: (sessionId: string, cwd: string) => void` - atomically sets `navigationView: 'session-timeline'`, `selectedSessionId: sessionId`, `currentDirectoryCwd: cwd`, `viewMode: 'graph'`, `treePanelOpen: false`, `focusedNodeId: null`. Do NOT clear `selectedNodeData` or `selectedGroupId` (per research pitfall 5).
- `exitToDirectory: () => void` - atomically sets `navigationView: 'directory'`, `selectedSessionId: null`, `currentDirectoryCwd: null`, `treePanelOpen: false`, `focusedNodeId: null`, `viewMode: 'directory'`. DO clear `selectedNodeData: null` and `selectedGroupId: null` on exit.
- `toggleTreePanel: () => void` - toggles `treePanelOpen` boolean.
- `setFocusedNode: (nodeId: string | null) => void` - sets `focusedNodeId`.

**Modify existing `loadViewMode()`:** Change the default return from `'tree'` to `'directory'` since directory is now the landing view. Keep localStorage persistence but the initial default should be `'directory'`.

**Keep `ViewMode` type** as-is (`'tree' | 'graph' | 'directory'`) since it's still used internally by GraphView and TreeView. The new `navigationView` is a higher-level navigation concept that coexists with `viewMode`.

Add new type `NavigationView = 'directory' | 'session-timeline'` exported from the store.

Add to SessionStore interface all new fields and actions. Implement in the create() call.
  </action>
  <verify>Run `npm run build` from project root. TypeScript compilation must succeed with no errors related to sessionStore.</verify>
  <done>SessionStore has navigationView, treePanelOpen, focusedNodeId, currentDirectoryCwd state fields and enterSession, exitToDirectory, toggleTreePanel, setFocusedNode actions. Default view is 'directory'.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure Layout, build Toolbar, update FilterBar and App</name>
  <files>client/src/components/Layout.tsx, client/src/components/Toolbar.tsx, client/src/components/FilterBar.tsx, client/src/App.tsx</files>
  <action>
**Layout.tsx - Complete rewrite:**
Remove the `sidebar` prop entirely. The new layout has:
- A header containing `<Toolbar />` (imported from `./Toolbar`)
- A main content area that is full-width (no sidebar column)
- Keep existing style patterns: dark theme colors (#1a1a2e, #16213e, #0f3460), inline styles, flex layout
- The `children` prop renders the center graph content
- Do NOT render tree panel here (that's plan 02). Just remove sidebar and wire toolbar.

New LayoutProps: `{ children: React.ReactNode }` (no sidebar, no headerActions)

```
Structure:
<div container (full viewport)>
  <header> <Toolbar /> </header>
  <main (flex: 1, overflow: hidden)>
    <div content (flex: 1, overflow: auto, NO padding - graphs need full width/height)>
      {children}
    </div>
  </main>
</div>
```

Remove `padding: '20px'` from the content style - the graph views need full width/height. The directory overview and graph view already handle their own sizing.

**Toolbar.tsx - New file:**
Create a new component that renders different controls based on `navigationView` from the store.

When `navigationView === 'directory'`:
- Left side: App title "Claude Session Dashboard" (keep existing #e94560 color)
- Right side: FilterBar (inline, not stacked), session count display showing "N sessions" (filtered count / total count if different), settings gear button
- No search, no view toggle per user decisions

When `navigationView === 'session-timeline'`:
- Left side: Back button (unicode left arrow or "< Back" text), session title from `getSessionTitle()` for the selected session
- Right side: Tree panel toggle button (tree icon or "Tree" text), session switcher `<select>` dropdown scoped to sessions in same directory (`currentDirectoryCwd`), settings gear button
- Session switcher uses `getSessionTitle()` for option labels, filters sessions by `session.cwd === currentDirectoryCwd`

Style the toolbar consistently with existing header style: `backgroundColor: '#16213e'`, `borderBottom: '1px solid #0f3460'`, padding `'8px 20px'`, flex row with space-between.

Toolbar buttons: Use similar styling to existing `settingsButton` in App.tsx - border: `1px solid #0f3460`, background: none, color: #888, hover: color #eee + borderColor #e94560.

Session count format (directory view): Show `"12 sessions"` or `"8 / 12 sessions"` when filters are active (8 filtered of 12 total).

Connection status indicator: Add the connected/disconnected dot from the current App.tsx sidebar to the toolbar (small dot indicator in the title area or toolbar edge).

**FilterBar.tsx - Modify:**
Remove the search input entirely (user decision: "Search functionality removed from directory view"). Keep only the Active/Archived checkboxes. Remove the `searchInput` style, the search-related state subscriptions, and the handleSearchChange callback. Remove the `setSearchTerm` import. Remove the bottom row that contains the search input.

Also make the FilterBar layout horizontal (single row) since it will be embedded in the toolbar, not stacked in a sidebar. Use `flexDirection: 'row'` and `gap: '10px'`. Remove `borderBottom` and reduce `padding` since it's now inside the toolbar.

**App.tsx - Major restructure:**
- Remove `SessionList` import and usage
- Remove `ViewToggle` import and usage
- Remove the `sidebar` variable entirely
- Remove the `headerActions` variable entirely (toolbar handles all header content)
- Move settings button to Toolbar (or pass settingsOpen/setSettingsOpen as needed - simplest: move Settings state management to Toolbar or keep in App and pass callback)
- `Layout` no longer takes `sidebar` or `headerActions` props - just `children`
- The `MainView` component should use `navigationView` from the store instead of `viewMode`:
  - When `navigationView === 'directory'`: render `<DirectoryOverview />`
  - When `navigationView === 'session-timeline'`: render `<GraphView />`
  - No more tree view top-level rendering (tree becomes a panel in plan 02)
- Keep: ErrorBoundary, GroupDrillDownPanel, Settings modal, WebSocket handling, notifications, favicon badge, tmux health check
- Pass `connected` and `tmuxAvailable` to Toolbar (or Settings) as needed. Simplest approach: keep health check in App, store connected state, pass to Toolbar via prop or add to Zustand store. Since the store pattern is established, the simplest is to pass as props to Toolbar or just move the settings button to stay in App.tsx header area.

Actually, for simplicity: keep the Settings button and modal in App.tsx. The Toolbar component will have a `rightSlot` or `actions` prop where App passes the settings button. OR, even simpler: move the settings gear into Toolbar and have Toolbar accept `onOpenSettings` callback prop + `connected` + `tmuxAvailable` boolean props.

Recommended approach for Toolbar props:
```typescript
interface ToolbarProps {
  connected: boolean;
  onOpenSettings: () => void;
}
```

This keeps settings state in App.tsx (where it already lives) and Toolbar just renders the button.

**Update DirectoryOverview.tsx click handler:**
Change the session node click handler from:
```
setSelectedSession(data.sessionId);
setViewMode('graph');
```
to:
```
const session = sessions.find(s => s.id === data.sessionId);
if (session) {
  enterSession(data.sessionId, session.cwd || '');
}
```
Import `enterSession` from the store. This is NAV-04.
  </action>
  <verify>Run `npm run build` from project root. TypeScript compilation must succeed. Then run `npm run dev` and verify in browser: (1) No left sidebar visible, (2) Directory graph shows as landing view with toolbar above, (3) Clicking a session node swaps to timeline graph, (4) Back button returns to directory, (5) Active/Archived filters work from toolbar, (6) Session count shows in toolbar.</verify>
  <done>Dashboard opens to directory graph with no sidebar. Toolbar shows filters + session count in directory view, and back + title + tree toggle + session switcher in session timeline view. Clicking session nodes navigates to timeline. Back button returns to directory. Settings gear accessible from toolbar. Connection status visible.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. Browser shows directory graph as landing view (no sidebar)
3. Toolbar displays filter checkboxes and session count in directory view
4. Clicking a session node in directory graph navigates to session timeline
5. Toolbar switches to show back button, session title, tree toggle, and session switcher
6. Back button returns to directory overview
7. Active/Archived filter checkboxes work (filtering sessions and hiding empty directories)
8. Session switcher dropdown shows only sessions from the same directory
9. Session count reflects filtered vs total count when filters are active
10. Settings gear button opens settings modal
</verification>

<success_criteria>
- NAV-01: Directory graph is the main view with no left sidebar session list visible
- NAV-02: Active/archived session filters work in directory graph toolbar
- NAV-04 (partial): Clicking session node navigates to session timeline graph
- Navigation state (enterSession/exitToDirectory) works atomically without state desync
- Toolbar adapts correctly between directory and session-timeline views
</success_criteria>

<output>
After completion, create `.planning/phases/12-navigation-refactor/12-01-SUMMARY.md`
</output>
