---
phase: 44-ux-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/TreeView.tsx
autonomous: true
---

<objective>
When the user clicks a tree node to navigate to it in the graph view, automatically turn off the "Follow End" toggle so the graph doesn't jump away from the navigated node.

Purpose: Prevent the follow-end auto-scroll from fighting with the user's deliberate navigation.
Output: Updated TreeView.tsx selectNode callback.
</objective>

<context>
@client/src/components/TreeView.tsx
@client/src/store/sessionStore.ts (read-only — do NOT modify)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-disable followPipelineEnd on tree node click</name>
  <files>client/src/components/TreeView.tsx</files>
  <action>
In TreeView.tsx:

1. Add `setFollowPipelineEnd` to the store selectors (around line 284 area, after the existing `setFocusedNode` selector):
   ```
   const setFollowPipelineEnd = useSessionStore((state) => state.setFollowPipelineEnd);
   ```

2. In the `selectNode` callback (line ~316), add `setFollowPipelineEnd(false)` as the FIRST line inside the callback body, before the existing `setSelectedNodeKey(nodeKey)` call. This ensures that any tree click immediately disables follow-end before setting the focused node, so GraphFocusHandler won't fight the navigation.

3. Add `setFollowPipelineEnd` to the useCallback dependency array for `selectNode` (line ~356).
  </action>
  <verify>Run `cd /home/botond/claude-session-dashboard && npm run build` — no TypeScript errors.</verify>
  <done>Clicking any tree node automatically turns off the "Follow End" toggle, preventing the graph from jumping away from the user's navigation target.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- Tree node click navigates graph to the clicked node AND disables follow-end
</verification>

<success_criteria>
Follow-end is automatically disabled when user clicks a tree node. No TypeScript errors.
</success_criteria>
