---
phase: 44-ux-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/Toolbar.tsx
autonomous: true
---

<objective>
Merge the separate "Expand All" and "Collapse All" buttons into a single toggle button, and apply a switch-like visual design to both that toggle and the "Follow End" toggle.

Purpose: Reduce toolbar clutter and give toggle buttons a modern switch appearance.
Output: Updated Toolbar.tsx with toggle + switch styling.
</objective>

<context>
@client/src/components/Toolbar.tsx
@client/src/store/sessionStore.ts (read-only — do NOT modify)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse-all toggle + switch design for both toggles</name>
  <files>client/src/components/Toolbar.tsx</files>
  <action>
1. **Collapse/Expand All toggle:** Replace the two separate buttons (lines ~421-451 "Expand All" / "Collapse All") with a single toggle button. The button should:
   - Derive an `allExpanded` boolean: check if selectedSession.subagents.length > 0 AND every subagent ID is in the expandedSubagentBoxes set for the current session. Use: `const sessionBoxes = expandedSubagentBoxes.get(selectedSessionId); const allExpanded = hasSubagents && selectedSession.subagents.every(s => sessionBoxes?.has(s.id));`
   - On click: if allExpanded, call `collapseAllSubagentBoxes(selectedSessionId)`. Else, call `expandAllSubagentBoxes(selectedSessionId, selectedSession.subagents.map(s => s.id))`.
   - Label: show agent-expand state ("Agents: ON" when expanded, "Agents: OFF" when collapsed).

2. **Switch-like design for both toggles (Follow End + Collapse toggle):** Add a new style called `switchButton` and `switchButtonActive` to the styles object. The switch design should look like:
   - Base: `padding: '4px 12px', fontSize: '12px', fontWeight: 500, background: '#1a1a2e', border: '1px solid #374151', borderRadius: '12px', color: '#6b7280', cursor: 'pointer', transition: 'all 0.2s', lineHeight: 1.2, display: 'flex', alignItems: 'center', gap: '6px'`
   - Active: same but with `background: '#052e16', border: '1px solid #16a34a', color: '#22c55e'`
   - Inside each switch button, render a small "dot" indicator: a 8x8 circle that is `#374151` when off and `#22c55e` when on, positioned before the label text.

3. **Apply switch design** to both buttons:
   - The Follow End button (currently lines ~452-476): replace button style with switchButton/switchButtonActive. Keep the existing onClick logic (shift+click to jump once, normal click to toggle). Change label to just "Follow End" (remove the unicode arrow). Active = followPipelineEnd.
   - The Agents toggle button: use switchButton/switchButtonActive. Active = allExpanded.

4. Remove the old handleButtonHover logic from both toggle buttons since the switch design handles active/inactive states via the style itself (no hover state needed for switches — they are either on or off).

Also need to read `expandedSubagentBoxes` from the store (already imported on line 222 area — verify it's accessed).
  </action>
  <verify>Run `cd /home/botond/claude-session-dashboard && npm run build` — no TypeScript errors.</verify>
  <done>Toolbar shows a single "Agents" toggle switch (only when session has subagents) and a "Follow End" toggle switch, both with on/off switch visual design (green dot + green border when active, gray when inactive). Separate Expand All / Collapse All buttons are gone.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- Toolbar in session view shows two switch-style toggles: "Agents" (when subagents exist) and "Follow End"
- Clicking Agents toggle expands all subagent boxes when off, collapses all when on
- Follow End toggle retains existing functionality (click to toggle, shift+click to jump once)
</verification>

<success_criteria>
Both toggle buttons use switch-like design. Collapse/Expand all is a single toggle. No TypeScript errors.
</success_criteria>
